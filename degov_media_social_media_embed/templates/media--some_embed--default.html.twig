{#
/**
 * @file
 * Default theme implementation to present a media entity.
 *
 * Available variables:
 * - name: Name of the media.
 * - content: Media content.
 *
 * @see template_preprocess_media()
 *
 * @ingroup themeable
 */
#}
{%
set media_classes = [
    'some-embed',
    'clearfix',
]
%}
<article{{ attributes.addClass(media_classes) }}>
  {% if social_media_source_disabled %}
  <div class="some-embed__code social-media-source-target"{{ content.field_some_embed_code|quickedit_attr }}>{{ 'Disabled'|trans }}</div>
  {% else %}
  <div class="some-embed__code"{{ content.field_some_embed_code|quickedit_attr }}>{{ content.field_some_embed_code|field_value }}</div>
  {% endif %}
</article>